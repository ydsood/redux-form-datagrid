import { PaginationHandler } from 'redux-form-datagrid';
import type { PaginationProps } from 'redux-form-datagrid';

export const next = (currentStart: number, pageSize: number) => { }
export const prev = (currentStart: number, pageSize: number) => { }
export const lastPage = (pageSize: number) => { }
export const firstPage = (pageSize: number) => { }